@model IEnumerable<DatabaseAccess.usertype>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.usertype_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.usertype_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.usertype_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.usertype_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.usertype_id })
        </td>
    </tr>
}

</table>
